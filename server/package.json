{
  "name": "anjam-server",
  "version": "1.2.1",
  "private": true,
  "description": "Anjam sync server (accounts + item sync)",
  "main": "server.js",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "nodemailer": "^10.0.10"
  }
}
