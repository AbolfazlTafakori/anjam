# /etc/nginx/conf.d/anjam-zones.conf — rate-limit zones for Anjam only (unique anjam_ prefix).
limit_req_zone $binary_remote_addr zone=anjam_auth:1m rate=10r/m;
limit_req_zone $binary_remote_addr zone=anjam_api:5m rate=20r/s;
